// var chalk = require('chalk'); // can color the output to the console
import chalk from 'chalk';  // babel-node allows the use of modern syntax

console.log(chalk.green('Starting app in dev mode...')); // eslint-disable-line no-console
